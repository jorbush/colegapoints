---
import { formatPoints } from '../lib/points';
import { getLocale, useTranslation } from '../lib/i18n';

const locale = getLocale(Astro.cookies, Astro.request.headers);
const t = useTranslation(locale);

interface Member {
  id: string;
  name: string;
  avatarEmoji: string;
  joinedAt: number | Date;
}

interface Props {
  members: Member[];
  pointTotals: Record<string, number>;
}

const { members, pointTotals } = Astro.props;

// Sort members by points descending for leaderboard
const sorted = [...members].sort((a, b) => (pointTotals[b.id] ?? 0) - (pointTotals[a.id] ?? 0));
---

<section class="mb-12">
  <h2
    class="mb-6 inline-block border-2 border-[var(--border)] bg-[var(--accent)] px-2 text-sm font-black tracking-widest uppercase"
  >
    {t('leaderboard.title')}
  </h2>
  {
    sorted.length === 0 ? (
      <div class="card p-12 text-center">
        <div class="mb-4 text-6xl">🏜️</div>
        <p class="text-2xl font-black uppercase italic">{t('leaderboard.no_colegas')}</p>
        <p class="mt-2 text-lg font-bold">{t('leaderboard.no_colegas_desc')}</p>
      </div>
    ) : (
      <ul class="stagger flex flex-col gap-4">
        {sorted.map((m, i) => {
          const pts = pointTotals[m.id] ?? 0;
          const isTop = i === 0 && pts > 0;
          const medalEmojis = ['🥇', '🥈', '🥉'];
          const medal = medalEmojis[i] ?? null;
          return (
            <li
              id={`member-${m.id}`}
              class={`card animate-slide-up flex cursor-pointer items-center gap-3 p-4 transition-all duration-150 hover:-translate-x-1 hover:-translate-y-1 hover:shadow-[7px_7px_0px_var(--border)] active:translate-x-0.5 active:translate-y-0.5 active:shadow-[3px_3px_0px_var(--border)] sm:gap-5 sm:p-5 ${isTop ? 'bg-[var(--accent-light)]' : ''}`}
              data-member-id={m.id}
              data-action="view-detail"
            >
              <div class="flex h-14 w-14 flex-shrink-0 items-center justify-center border-4 border-[var(--border)] bg-white text-3xl shadow-[3px_3px_0px_var(--border)] sm:h-16 sm:w-16 sm:text-4xl">
                {m.avatarEmoji}
              </div>
              <div class="min-w-0 flex-1">
                <div class="flex flex-wrap items-center gap-x-2 gap-y-1">
                  <span class="max-w-full truncate text-xl font-black tracking-tight uppercase sm:text-2xl">
                    {m.name}
                  </span>
                  <div class="flex flex-shrink-0 items-center gap-1.5">
                    {medal && <span class="text-xl">{medal}</span>}
                    {isTop && <span class="tag tag-brand">{t('leaderboard.top_colega')}</span>}
                  </div>
                </div>
                <div class="mt-1 text-sm font-bold italic">
                  {t('leaderboard.joined', {
                    date: new Date(m.joinedAt).toLocaleDateString(locale),
                  })}
                </div>
              </div>
              <div
                class={`border-4 border-[var(--border)] px-2.5 py-1 text-2xl font-black tabular-nums shadow-[3px_3px_0px_var(--border)] sm:px-4 sm:py-2 sm:text-4xl ${pts > 0 ? 'bg-[var(--positive-bg)] text-[var(--positive)]' : pts < 0 ? 'bg-[var(--negative-bg)] text-[var(--negative)]' : 'bg-white text-[var(--muted)]'}`}
              >
                {pts > 0 ? `+${formatPoints(pts)}` : formatPoints(pts)}
              </div>
            </li>
          );
        })}
      </ul>
    )
  }
</section>
